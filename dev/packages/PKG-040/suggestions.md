💡 SUGGESTION: DONE checklist references `build/server/index.js`; adapter-node output uses `build/index.js` + `build/server/chunks/` — align verify step with `scripts/verify-build.cjs`.

💡 SUGGESTION: Legacy `→ [Done|Escalate]` syntax in action-queue.md and research PROPOSE lines now falls back to column defaults. Migrate existing lines to `→ [A: label|B: label]` or remove trailing syntax.

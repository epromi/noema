## TASK

Package: PKG_PLACEHOLDER — PKG_NAME_PLACEHOLDER
Size: PKG_SIZE_PLACEHOLDER | Effort: PKG_EFFORT_PLACEHOLDER

Read the spec:  `dev/packages/PKG_PLACEHOLDER/spec.md`
Read the rules: `projects/noema/CONTRIBUTING.md`

Files to create or modify (from spec Scope):
PKG_FILES_PLACEHOLDER

## ARCHITECTURE — break any of these and the implementation is rejected

1. `src/lib/core/` = plain TS only. NO Svelte imports. NO browser APIs (`window`, `document`).
2. `src/lib/components/` = Svelte UI only. Receives data via props from parent page/component.
3. Data flow is ONE-WAY: provider → core → component. Components NEVER call core directly. Core NEVER imports components.
4. Every changed or new `src/lib/core/*.ts` module MUST have tests with ≥70% coverage on the new code.
   Scope: `pnpm test -- --coverage --collectCoverageFrom='src/lib/core/NEW_MODULE.ts'`.
5. Max 5 files created or modified per phase. If a phase genuinely needs more, split it into sub-phases (F2a/F2b) — but implement EVERYTHING the spec requires.

## REFERENCE — follow these patterns

- **Every new module:** open `src/lib/core/health.ts` + `tests/core/health.test.ts`
  Copy: provider injection, Promise.all, typed returns, try/catch at module boundary, mock setup, edge case tests
- **Every new UI component:** open `src/lib/components/shared/CpuWidget.svelte`
  Copy: `export let` props, reactive `$:` values, color thresholds, semantic HTML
- For anything else: `grep` the codebase for similar code — the pattern is there.

## PLAN — plan briefly, then implement

Answer before coding (3-5 bullets):
1. What functions or components will you add?
2. What is the data flow? (provider → which core fn → which component?)
3. What is the SIMPLEST version that satisfies the spec?

If research findings exist below, incorporate them into the plan. If the RESEARCH FINDINGS section shows "(none)" or is empty, skip this step — no search is needed.

## IMPLEMENTATION — one phase at a time, no skipping

**Every phase gate (ALL must pass to proceed):**
  a. Write code (≤5 files)
  b. `pnpm check` (TypeScript + Svelte checking) → zero errors
  c. `pnpm test` → zero failures. If your change breaks old tests, fix YOUR implementation — not the test. Only modify an existing test if the test itself is demonstrably buggy.
  d. `pnpm build` → clean output, no warnings
  e. Verify: `ls build/server/index.js` exists (build output must be complete)

**Phase F1: Core Logic**
- Files in `src/lib/core/` + `tests/core/`
- Write the test FIRST → run `pnpm test` → confirm it fails (no implementation yet) → then implement until it passes
- Run the phase gate

**Phase F2: UI Components**
- Files in `src/lib/components/`
- Components must work in isolation: export props, render without side effects
- Wiring into `+page.svelte` or tab components is allowed ONLY if the spec explicitly lists those files in its Scope
- Run the phase gate

**Phases beyond F2** — follow the spec's phase headings regardless of number (F3, F4, F5, etc.):
- Each phase runs the same gate (code → check → test → build → verify)

## GARDENING — allowed ONLY in files you create or modify

**Allowed:** fix `any` → concrete types, add try/catch at module boundaries only, add `alt`/`aria-label`/`role`, remove dead imports, fix TODO/FIXME comments YOU introduced, deduplicate code within the same file only when the logic is IDENTICAL in intent (not just structurally similar).

**Forbidden:** change API contracts (props, params, responses), change CSS design tokens (`--accent`, `--bg`, `--border`, `--text`), remove or rename existing features, add npm dependencies, change routing, touch files outside package scope, modify code marked `// STABLE: do not modify`.

If you see a larger improvement opportunity, log it: `💡 SUGGESTION: <brief description>` to `dev/packages/PKG_PLACEHOLDER/suggestions.md`.

## RESEARCH FINDINGS

RESEARCH_PLACEHOLDER

## DONE — all must be true before you stop

- [ ] `pnpm check` — zero errors, zero warnings
- [ ] `pnpm test`  — all tests pass (your tests + pre-existing). If old tests broke, your IMPLEMENTATION is wrong — fix it, not the test.
- [ ] Coverage on files YOU created or modified: ≥70%
      `pnpm test -- --coverage --collectCoverageFrom='src/lib/core/YOUR_MODULE.ts'`
- [ ] `pnpm build` — clean output, zero errors
- [ ] `ls build/server/index.js` — build artifact exists (no missing chunks)
- [ ] Every spec requirement (all phases) is implemented
- [ ] No files outside the package scope were modified
- [ ] `💡 SUGGESTION` logged to `dev/packages/PKG_PLACEHOLDER/suggestions.md` (if any)

## COMMIT — single commit when all DONE checks pass

Run:
```
git add src/ tests/ dev/packages/
git commit -m "🧠 feat: SHORT_SUMMARY (PKG-PLACEHOLDER)

- Bullet list of what changed

✅ check | test | build | coverage"
```
Never commit generated files (`.svelte-kit/`, `build/`, `node_modules/`).

## TROUBLESHOOTING

| Symptom | Most likely fix |
|---------|----------------|
| `pnpm check` fails | Read TS error → fix affected file → re-run |
| `pnpm test` fails | Your implementation broke behavior → fix YOUR code, not the test |
| `pnpm build` fails | Usually a missing export → check `src/lib/core/index.ts` re-exports all new modules |
| Spec ambiguous or contradictory | `⛔ BLOCKED: <reason>` and STOP — do not guess |
